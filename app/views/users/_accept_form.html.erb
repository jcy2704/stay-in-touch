<%= form_tag(friendship_path(invites[:was_invited].find_by(user_id: user.id).id), method: :patch) do |f| %>
  <%= submit_tag  'Accept' %>
<% end %>
<%= form_tag(friendship_path(invites[:was_invited].find_by(user_id: user.id).id), method: :delete) do |f| %>
  <%= submit_tag  'Decline', data: { confirm: 'Are you sure you want to decline?' } %>
<% end %>